type TeamHasMember {
    id: ID!
    team_id: ID!
    user_id: ID!
}

input NewTeamHasMember {
    team_id: ID!
    user_id: ID!
}

input NewTeamHasMemberByEmail {
    team_id: ID!
    email: String!
}
type Board {
    id: ID!
    name: String!
    created_at: Time!
    updated_at: Time
    team_id: ID!
}

input NewBoard {
    name: String!
    team_id: ID!
}

type BoardOps {
    create(input: NewBoard!): Board! @goField(forceResolver: true) @isLogin
}
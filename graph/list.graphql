type List {
    id: ID!
    name: String!
    created_at: Time!
    updated_at: Time
    board_id: ID!
}

input NewList {
    name: String!
    board_id: ID!
}

type ListOps {
    create(input: NewList!): List! @goField(forceResolver: true)
}